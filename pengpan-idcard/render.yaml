services:
  - name: default
    type: web
    env: docker
    runtime: image
    image:
      url: docker.io/pengpan/idcard:latest
    startCommand: ""
    plan: starter
    region: oregon
    envVars:
      - key: PORT
        value: "8080"
