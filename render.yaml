services:
  - name: default
    type: web
    env: docker
    runtime: image
    image:
      url: ghcr.io/minimalcompact/thumbor:latest
    startCommand: ""
    plan: starter
    region: oregon
    envVars:
      - key: PORT
        value: "80"
