services:
  - name: default
    type: web
    env: docker
    runtime: image
    image:
      url: docker.io/b4bz/homer:latest
    startCommand: ""
    plan: free
    region: oregon
    envVars:
      - key: PORT
        value: "8080"
